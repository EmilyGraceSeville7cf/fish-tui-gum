const fromRaw = "Equal contents expected for objects created through constructor and via `fromRaw` method"
const toString = "Correctly formatted string representation expected for object"

module.exports = {
    fromRaw,
    toString
}
