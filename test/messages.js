const fromRaw = "Expected equal contents expected for objects created through constructor and via `fromRaw` method"
const toString = "Expected correctly formatted string representation expected for object"

module.exports = {
    fromRaw,
    toString
}
